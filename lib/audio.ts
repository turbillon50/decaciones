import type { Track } from "@/lib/types";

export function getTrackAudioSrc(_track: Track): string {
  return "";
}

export function withAudioCategory(tracks: Track[], _category: string): Track[] {
  return tracks;
}
